/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  output: "export",

  basePath: "/website-template",
  assetPrefix: "/website-template/",
};

module.exports = nextConfig;